[workspace]
resolver = "2"
members = [
  "kafka-protocol",
  # "kafka-io",
  "kafka-core",
  #  "kafka-producer",
  # "kafka-consumer",
  # "kafka-admin",      # enable when Milestone-6 starts
]

[package]
name = "kafka4rs"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
authors = ["Arend-Jan <arendjan.oosterveld@gmail.com>"]
homepage = "https://github.com/Arend-Jan/kafka4rs"
repository = "https://github.com/Arend-Jan/kafka4rs"
documentation = "https://docs.rs/kafka4rs"
description = "Pure‑Rust client for Apache Kafka 4.0+ – drop‑in replacement for librdkafka, powered by Tokio and zero‑copy buffers."
keywords = ["kafka", "streaming", "messaging", "async", "rust"]
categories = ["asynchronous", "network-programming"]

[dependencies]


[badges]
maintenance = { status = "experimental" }
